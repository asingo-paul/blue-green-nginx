# Example environment variables for Blue/Green Deployment
# -------------------------------------------------------
# This file defines all parameters required for running
# the Blue/Green Node.js service behind Nginx.
# Copy this file to `.env` and set your own values if needed.

# --- Application Images ---
# Use the official pre-built Blue and Green Node.js images (do NOT rebuild)
BLUE_IMAGE=ghcr.io/hngprojects/hngx-bluegreen-blue:latest
GREEN_IMAGE=ghcr.io/hngprojects/hngx-bluegreen-green:latest


# --- Active Pool Control ---
# Determines which pool is currently active (blue or green)
ACTIVE_POOL=blue

# --- Release IDs ---
# These identifiers appear in response headers (X-Release-Id)
RELEASE_ID_BLUE=blue-release-1
RELEASE_ID_GREEN=green-release-1

# --- App Port ---
# The internal app port used by Node.js apps (must match Dockerfile EXPOSE)
PORT=8080
